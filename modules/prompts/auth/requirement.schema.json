{
  "module": "auth/requirement",
  "version": "1.0.0",
  "description": "Defines mandatory authorization requirements that the agent must enforce before proceeding",
  "parameters": {
    "verification_methods": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of accepted identity verification methods presented to the user",
      "required": true,
      "examples": [
        ["SMS verification code"],
        ["SMS verification code", "bank card last 4 digits"]
      ]
    },
    "timeout_seconds": {
      "type": "integer",
      "description": "Number of seconds after which an inactive verification session expires",
      "required": false,
      "default": 300,
      "minimum": 60,
      "maximum": 3600
    }
  }
}
