{
  "module": "input/intent-parsing",
  "version": "1.0.0",
  "description": "Guides the agent to identify user intent and extract entities from input",
  "parameters": {
    "domain": {
      "type": "string",
      "description": "The business domain the agent operates in (e.g. 'loan application', 'customer support')",
      "required": true,
      "examples": ["loan application", "flight booking", "customer support"]
    },
    "supported_intents": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string" },
          "description": { "type": "string" }
        },
        "required": ["id", "description"]
      },
      "description": "List of intents the agent can handle",
      "required": true
    },
    "supported_languages": {
      "type": "array",
      "items": { "type": "string" },
      "description": "BCP-47 language tags for supported input languages",
      "required": false,
      "default": ["zh-CN", "en"]
    }
  }
}
