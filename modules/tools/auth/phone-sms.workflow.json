{
  "workflow": "auth/phone-sms",
  "version": "1.0.0",
  "description": "Sends an SMS verification code to the user's phone number and verifies the code they provide",
  "parameters": {
    "phone_number_variable": {
      "type": "string",
      "description": "Canvas variable name that holds the user's phone number",
      "required": true,
      "example": "user_phone"
    },
    "code_length": {
      "type": "integer",
      "description": "Length of the generated verification code",
      "required": false,
      "default": 6,
      "enum": [4, 6]
    },
    "expiry_seconds": {
      "type": "integer",
      "description": "Number of seconds before the verification code expires",
      "required": false,
      "default": 300
    }
  },
  "inputs": {
    "phone_number": "{{phone_number_variable}}"
  },
  "outputs": {
    "verified": {
      "type": "boolean",
      "description": "Whether the user successfully verified their phone number"
    },
    "error_code": {
      "type": "string",
      "description": "Error code if verification failed (e.g. 'code_expired', 'max_attempts_exceeded')",
      "nullable": true
    }
  },
  "nodes": []
}
